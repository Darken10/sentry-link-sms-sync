<resources>
    <!-- Base application theme - SentryLink bleu marine + cyan -->
    <style name="Base.Theme.SentryLink" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
    </style>

    <style name="Theme.SentryLink" parent="Base.Theme.SentryLink" />

    <!-- Thème Splash Screen : plein écran, fond bleu marine comme le logo -->
    <style name="Theme.SentryLink.Splash" parent="Base.Theme.SentryLink">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Style pour les cartes du Home -->
    <style name="HomeCardStyle">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">8dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <!-- Style bouton SOS -->
    <style name="SosButtonStyle" parent="Widget.Material3.Button">
        <item name="android:textSize">36sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/sos_red</item>
    </style>
</resources>

